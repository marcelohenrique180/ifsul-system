role(id, role)

usuario(id, email, senha, #role)

notificacao(id, mensagem, #usuario)

departamento(id, nome, #usuario)

corac(id, #departamento);

cord_curso(id, #departamento);

aluno(id, nome, rg, data_nasc, matricula, telefone, #usuario)

curso(id, nome)

aluno_disciplina(id, #aluno, #curso)

requerimento(id, justificativa, status, #aluno, #deferinte, #atual)

parecer(id, deferido, parecer #requerimento)
